# ---------------------------------------------------------------------------
# Workspace rule: this folder is BOTH the private research workspace and the
# code repo. Code is built here at the root, beside the research PDFs.
# Nothing private below is ever pushed. Verify with `git ls-files` before any
# push, and never `git add -A`.
# ---------------------------------------------------------------------------

# Internal research / strategy documents (stay local, never public)
*.pdf
BUILD_PLAN.md
BUILD_PLAN copy.md

# Secrets
.env
.env.*
!.env.example
*.pem
*.key
service-account*.json
.npmrc
.pypirc
*.jks
*.keystore
keystore.properties

# Dependencies
node_modules/
.pnp
.pnp.js
.venv/
venv/

# Build output
.next/
out/
.mobile-build-hold/
dist/
build/
.turbo/
*.tsbuildinfo
.vercel/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# Native build variants
ios/App/Pods/
android/.gradle/
android/app/build/
*.ipa
*.apk
*.aab

# Corpus intermediates (frozen artifacts are committed deliberately, raw is not)
pipeline/raw/
pipeline/tmp/

# Vendored third-party reference code, research-only, never shipped
pipeline/orbitm_vendor/

# OS / editor
.DS_Store
.AppleDouble
._*
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Private planning docs (stay local, never public)
CLAUDE.md
stephen-impl-plan.md
BOB_HANDOFF.md

# Model weight and benchmark caches, never committed
pipeline/.surya_weights/
pipeline/.surya_bench/

# Corpus binary artifacts - built in CI, stored in Vercel Blob (Q6 decision)
corpus/manifest.sqlite
corpus/vectors.f32
eval/report.json

# Serwist build output (generated by next build, task 2.10)
public/sw.js
public/swe-worker*.js

# Context Forge gateway local database (task 3.2, never committed)
mcp.db*
.venv-forge/
